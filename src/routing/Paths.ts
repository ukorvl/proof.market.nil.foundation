/**
 * @file Enum declaration.
 * @copyright Yury Korotovskikh <u.korotovskiy@nil.foundation>
 */

/**
 * Paths.
 */
export enum Path {
    root = '/',
    any = '*',
    login = '/login',
    portfolio = '/portfolio',
    register = '/register',
    market = '/market',
    requests = 'requests',
    proposals = 'proposals',
    statements = 'statements',
    charts = '/charts',
}
